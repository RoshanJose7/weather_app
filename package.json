{
  "name": "weather_app",
  "version": "1.0.0",
  "description": "a weather app using some random api.",
  "main": "src/index.js",
  "scripts": {
    "prestart": "(cls || clear) && echo Installing Dependencies...... && npm install",
    "start": "(cls || clear) && echo Pruning Dependencies...... && node prune --production",
    "pretest": "(cls || clear) && echo Installing Dependencies...... && npm install",
    "test": "(cls || clear) && set NODE_ENV=development && nodemon src/index.js"
  },
  "author": "Jose",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "hbs": "^4.1.1",
    "request": "^2.88.2"
  }
}
